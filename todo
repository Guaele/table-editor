---insert should insert new rows if user wants to.

	example:

 		1, 2|
		3, 4|
		
	insert 3 1 newElem
	
		1, 2|
		3, 4|
		new Elem, |

	insert 3 2 other
	
		1, 2|
		3, 4|
		new Elem, other|

--- Need to take care of empty spaces if row or col added.

--- Save table in file.

--- Able to create tables from start.

--- User chooses if it wants to start a new table or to load a file.
